target-version = "py311"
line-length = 100

[lint]
select = ["E", "F", "I", "UP"]
ignore = ["E501"]

[lint.per-file-ignores]
"main.py" = ["F401"]
# Env must be set before importing app (engine binds at import time).
"tests/conftest.py" = ["E402"]

[format]
quote-style = "double"
